commands:
    00httpd:
        command: mkdir /var/log/httpd
    01tomcat8:
        command: mkdir /var/log/tomcat8
    02restart:
        command: service httpd stop && service tomcat8 restart && service httpd start
